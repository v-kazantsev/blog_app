<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :Title %>
    <%= form.text_field :title %>
  </div>
  <div>
    <%= form.label :Source %>
    <%= form.text_area :source, rows: 5, cols: 75 %>
  </div>
  <div>
    <%= form.label :Body %>
    <%= form.text_area :body, rows: 15, cols: 75 %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
